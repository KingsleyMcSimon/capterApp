<%= simple_form_for(@capt) do |f|  %>
  <%= f.error_notification  %>
  <div class="field">
  <div class="control">
  <%= f.input :capt, label: "Capt about it", input_html: { class: "textarea " }, wrapper: false, label_html: {class: "label" }, placeholder: "Compose a new conversation...capt ",  autofocus: true %>
  </div>
  </div>
  <%= f.button :submit, class: "button is-info" %>
  <% end %>